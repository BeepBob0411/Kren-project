// lib/pages/mitigasi/banjir.dart

import 'package:flutter/material.dart';

class BanjirPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mitigasi Banjir'),
      ),
      body: Center(
        child: Text('Halaman Mitigasi Banjir'),
      ),
    );
  }
}
