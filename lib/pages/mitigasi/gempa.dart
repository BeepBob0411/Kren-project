// lib/pages/mitigasi/gempa.dart

import 'package:flutter/material.dart';

class GempaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mitigasi Gempa'),
      ),
      body: Center(
        child: Text('Halaman Mitigasi Gempa'),
      ),
    );
  }
}
